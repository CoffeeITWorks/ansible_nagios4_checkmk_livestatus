---
galaxy_info:
  author: Pablo Estigarribia
  description: Install check-mk-livestatus and configs nagios
  license: GPLv2
  min_ansible_version: 2.0
  platforms:
  - name: Ubuntu
    versions:
    - trusty
  galaxy_tags:
  - Monitoring
  - Infraestructure
allow_duplicates: no
# Dependencies are only commented due that I prefer to explicit define it on my site.yml
#dependencies:
#  - { role: ANXS.mysql, tags: ["install", "nagios_server_all", "nagios_server_thruk", "ANXS.mysql"] }